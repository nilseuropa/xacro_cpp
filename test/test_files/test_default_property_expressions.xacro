<a xmlns:xacro="http://www.ros.org/xacro">
    <xacro:property name="prop" default="2"/>
    <xacro:property name="plist" default="[10, 20, 30]"/>

    <xacro:if value="${prop + 1 == 3}">
        <foo val="${prop + 1}"/>
    </xacro:if>

    <bar val="${plist[1]}"/>
</a>
